# AGENTS.md

## Repository Context
- This repository is a minimal Git demo workspace.
- Current workspace content is documentation-only (`gitinitdemosample.md`).
- No build, test, lint, or runtime toolchain is configured.

## Agent Working Rules
- Prefer Git workflow help and documentation edits over code scaffolding.
- Do not assume language/tooling conventions (Node, Python, etc.) unless new project files are added.
- Before suggesting automation commands, verify required files exist (for example, `package.json`, `pyproject.toml`, `Makefile`).
- Keep changes minimal and task-focused; avoid introducing unrelated project structure.

## Git-Specific Notes
- Remote is configured as `orgin` (intentional current state in local config), not `origin`.
- Branch tracking currently targets `master` via remote `orgin`.
- Do not rename remotes or branches unless explicitly requested.

## Reference
- Project note: [gitinitdemosample.md](gitinitdemosample.md)